#pragma once

// ============================================================================
// SimpleCommKit — Umbrella Header (auto-generated by CMake)
//
// Include this single header to use all enabled SimpleCommKit modules.
// The set of included modules is controlled by CMake options:
//   ENABLE_SIMPLECOMMKIT_BLE,             ENABLE_SIMPLECOMMKIT_HID,
//   ENABLE_SIMPLECOMMKIT_USB,             ENABLE_SIMPLECOMMKIT_SERIALPORT,
//   ENABLE_SIMPLECOMMKIT_TCP,             ENABLE_SIMPLECOMMKIT_UDP,
//   ENABLE_SIMPLECOMMKIT_WEBSOCKET,       ENABLE_SIMPLECOMMKIT_MQTTCLIENT
// ============================================================================

// ============================================================================
// Version — auto-generated from VERSION file
// ============================================================================

#define SIMPLECOMMKIT_VERSION       "@SIMPLECOMMKIT_VERSION@"
#define SIMPLECOMMKIT_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define SIMPLECOMMKIT_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define SIMPLECOMMKIT_VERSION_PATCH @PROJECT_VERSION_PATCH@

#include <cstdint>
#include <string>

namespace SimpleCommKit {

struct SimpleCommKitVersion {
    uint32_t major = SIMPLECOMMKIT_VERSION_MAJOR;
    uint32_t minor = SIMPLECOMMKIT_VERSION_MINOR;
    uint32_t patch = SIMPLECOMMKIT_VERSION_PATCH;

    std::string to_string() const {
        return std::to_string(major) + "." +
               std::to_string(minor) + "." +
               std::to_string(patch);
    }
};

/// 获取完整版本号字符串，如 "0.1.0"
inline const char* getSimpleCommKitVersion() {
    return SIMPLECOMMKIT_VERSION;
}

/// 获取版本结构体
inline SimpleCommKitVersion getSimpleCommKitVersionInfo() {
    return {};
}

} // namespace SimpleCommKit

// ============================================================================
// Common headers
// ============================================================================

#include <SimpleCommKitErrorMap.hpp>
#include <SimpleCommKitExport.h>
@SIMPLE_COMMKIT_MODULE_INCLUDES@
